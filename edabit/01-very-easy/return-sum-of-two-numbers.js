/**
 * Create a function that takes two numbers as arguments and return their sum.
 *
 * LEVEL: very easy
 */

function addition(a, b) {
  return a + b;
}

addition(3, 2); // 5
addition(-3, -6); // -9
addition(7, 3); // 10
